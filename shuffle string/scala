object Solution {
def restoreString(s: String, indices: Array[Int]): String = {
    var word:String ="";
    for (a <- 0 to (indices.length-1)){
      word=word+s(indices.indexOf(a));
    }
    return word;
  }
}
